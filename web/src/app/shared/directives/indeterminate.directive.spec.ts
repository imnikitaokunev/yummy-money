import { IndeterminateDirective } from './indeterminate.directive';

describe('IndeterminateDirectiveDirective', () => {
    it('should create an instance', () => {
        const directive = new IndeterminateDirective();
        expect(directive).toBeTruthy();
    });
});
